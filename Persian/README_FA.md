<div dir="rtl" align="right">

# آموزش مقدماتی C++ — جلسهٔ یکپارچه (تا قبل از مبحث «آرایه»)
> این فایل برای آموزش مفاهیم پایهٔ C++ به نوآموزان طراحی شده است و شامل توضیحات کامل و مثال‌های ساده است تا بتوانید در یک جلسهٔ آموزشی همهٔ مفاهیم اولیه را ارائه دهید. در این نسخه، **مباحث توابع، آرایه، رشته و وکتور** حذف شده‌اند.

---

## 🎯 اهداف آموزش
- درک ساختار کلی برنامهٔ C++ و نحوهٔ اجرای آن.
- یادگیری تعریف متغیرها و انواع داده‌های پایه.
- آشنایی با عملگرها، به‌ویژه عملگرهای افزایشی و کاهشی.
- آشنایی با شرط‌ها (`if`, `else`, `switch`) و حلقه‌ها (`for`, `while`, `do-while`).

---

## 🧩 1) مقدمه و ساختار پایهٔ برنامه
C++ یکی از زبان‌های قدرتمند و پرکاربرد در حوزه‌های مختلف مانند مهندسی، شبیه‌سازی و بازی‌سازی است. هر برنامهٔ C++ از تابع `main()` شروع می‌شود و معمولاً شامل دستوراتی برای ورودی و خروجی داده‌هاست.

<div dir="ltr" align="left">

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello World!" << endl;
    return 0;
}
```
</div>

**توضیحات:**
- `#include <iostream>` برای استفاده از دستورات ورودی و خروجی است.
- `using namespace std;` باعث می‌شود مجبور نباشیم جلوی `cout` یا `cin` بنویسیم `std::`.
- `main()` نقطهٔ شروع برنامه است.
- `return 0;` یعنی برنامه با موفقیت اجرا شده است.

---

## 💬 2) ورودی و خروجی (Input / Output)
در C++ برای چاپ داده از `cout` و برای دریافت داده از `cin` استفاده می‌شود.

<div dir="ltr" align="left">

```cpp
#include <iostream>
using namespace std;

int main() {
    string name;
    int age;
    cout << "Enter your name: ";
    cin >> name;
    cout << "Enter your age: ";
    cin >> age;
    cout << "Hello " << name << ", you are " << age << " years old." << endl;
    return 0;
}
```
</div>

> نکته: `cout` به معنی "console output" و `cin` به معنی "console input" است.

---

## 🧠 3) انواع داده‌ها و تعریف متغیرها
در C++ برای ذخیرهٔ داده‌ها از **متغیر (Variable)** استفاده می‌شود. هر متغیر باید نوع داده‌ای مشخصی داشته باشد.

<div dir="ltr" align="left">

| نوع داده | توضیح | مثال |
|-----------|--------|--------|
| `int` | عدد صحیح | `int x = 10;` |
| `double` | عدد اعشاری | `double pi = 3.14;` |
| `char` | یک کاراکتر | `char grade = 'A';` |
| `bool` | مقدار درست یا نادرست | `bool isOk = true;` |
| `string` | متن (کلمه یا جمله) | `string name = "Ali";` |

<div>

**نکات مهم در تعریف متغیر:**
- نام متغیر نباید با عدد شروع شود.
- حروف بزرگ و کوچک فرق دارند (`Age` و `age` متفاوت‌اند).
- می‌توان چند متغیر از یک نوع را باهم تعریف کرد:

<div dir="ltr" align="left">

```cpp
int a = 10, b = 20, c = 30;
```
</div>

**نمونه:**

<div dir="ltr" align="left">

```cpp
int number = 5;
double price = 12.5;
char letter = 'C';
bool passed = false;
```
</div>

---

## ➕ 4) عملگرها (Operators)
### الف) عملگرهای ریاضی
| عملگر | معنی | مثال |
|--------|--------|--------|
| `+` | جمع | `x + y` |
| `-` | تفریق | `x - y` |
| `*` | ضرب | `x * y` |
| `/` | تقسیم | `x / y` |
| `%` | باقیمانده تقسیم | `x % y` |

### ب) عملگرهای مقایسه‌ای
| عملگر | معنی | مثال |
|--------|--------|--------|
| `==` | برابر | `x == y` |
| `!=` | نابرابر | `x != y` |
| `>` | بزرگ‌تر | `x > y` |
| `<` | کوچک‌تر | `x < y` |
| `>=` | بزرگ‌تر یا مساوی | `x >= y` |
| `<=` | کوچک‌تر یا مساوی | `x <= y` |

### ج) عملگرهای منطقی
| عملگر | معنی | مثال |
|--------|--------|--------|
| `&&` | و (AND) | `(x > 0 && y > 0)` |
| `\|\|` | یا (OR) | `(x > 0 \|\| y > 0)` |
| `!` | نقیض (NOT) | `!(x > 0)` |

---

## 🔁 5) عملگرهای افزایشی و کاهشی (++ و --)
این عملگرها برای افزایش یا کاهش مقدار متغیر استفاده می‌شوند.

<div dir="ltr" align="left">

```cpp
int i = 5;
i++; // i = 6
++i; // i = 7
```
</div>

تفاوت بین پیشوند (`++i`) و پسوند (`i++`):
- `++i`: اول افزایش می‌دهد، بعد استفاده می‌کند.
- `i++`: اول استفاده می‌کند، بعد افزایش می‌دهد.

مثال:
<div dir="ltr" align="left">

```cpp
int a = 5;
cout << a++ << endl; // prints 5, then a becomes 6
cout << ++a << endl; // a becomes 7, then prints 7
```
</div>

به‌صورت مشابه:
<div dir="ltr" align="left">

```cpp
int b = 5;
b--; // b = 4
--b; // b = 3
```
</div>

---

## ⚙️ 6) شرط‌ها (if / else / switch)
برای تصمیم‌گیری در برنامه از شرط‌ها استفاده می‌شود.

<div dir="ltr" align="left">

```cpp
int number;
cout << "Enter a number: ";
cin >> number;

if (number > 0) {
    cout << "Positive number" << endl;
} else if (number < 0) {
    cout << "Negative number" << endl;
} else {
    cout << "Zero" << endl;
}
```
</div>

---

## 🔄 7) حلقه‌ها (Loops)
حلقه‌ها برای اجرای چندبارهٔ یک بخش از برنامه استفاده می‌شوند. در C++ سه نوع حلقهٔ اصلی وجود دارد:

### 🔹 حلقهٔ `for`
وقتی تعداد تکرار از قبل مشخص است، از `for` استفاده می‌شود.
<div dir="ltr" align="left">

```cpp
for (int i = 1; i <= 5; i++) {
    cout << i << " ";
}
```
</div>

### 🔹 حلقهٔ `while`
وقتی نمی‌دانیم دقیقاً چند بار باید تکرار شود ولی تا زمانی که شرط درست است می‌خواهیم ادامه یابد، از `while` استفاده می‌کنیم.
<div dir="ltr" align="left">

```cpp
int i = 1;
while (i <= 5) {
    cout << i << " ";
    i++;
}
```
</div>

### 🔹 حلقهٔ `do-while`
این حلقه شبیه `while` است با این تفاوت که **حتی اگر شرط از ابتدا نادرست باشد، دست‌کم یک بار اجرا می‌شود.**
<div dir="ltr" align="left">

```cpp
int i = 1;
do {
    cout << i << " ";
    i++;
} while (i <= 5);
```
</div>

### 🔹 دستورات `break` و `continue`
- `break`: خروج فوری از حلقه، حتی اگر شرط ادامه برقرار باشد.
- `continue`: پرش به تکرار بعدی حلقه، بدون اجرای دستورات بعد از آن در همان دور.

مثال:
<div dir="ltr" align="left">

```cpp
for (int i = 1; i <= 5; i++) {
    if (i == 3) continue; // skip printing 3
    if (i == 5) break;    // exit loop when i is 5
    cout << i << " ";
}
// output: 1 2 4
```
</div>

**تفاوت‌ها به‌طور خلاصه:**
| نوع حلقه | زمان بررسی شرط | کاربرد |
|-----------|----------------|----------|
| `for` | قبل از اجرا | وقتی تعداد دفعات تکرار مشخص است |
| `while` | قبل از اجرا | وقتی شرط ادامه از قبل مشخص نیست |
| `do-while` | بعد از اجرا | وقتی می‌خواهیم حداقل یک بار کد اجرا شود |

---

## 🧭 8) ساختار `switch-case`
دستور `switch` برای زمانی است که می‌خواهیم چند حالت ممکن از یک متغیر را بررسی کنیم (به‌جای چند `if-else`).

<div dir="ltr" align="left">

```cpp
int day = 3;
switch (day) {
    case 1:
        cout << "Saturday";
        break;
    case 2:
        cout << "Sunday";
        break;
    case 3:
        cout << "Monday";
        break;
    case 4:
        cout << "Tuesday";
        break;
    case 5:
        cout << "Wednesday";
        break;
    default:
        cout << "Invalid day";
}
```
</div>

---

## 📚 منابع مفید
- [W3Schools C++ Tutorial](https://www.w3schools.com/cpp/)
- cppreference.com برای توضیحات فنی و دقیق‌تر.

</div>